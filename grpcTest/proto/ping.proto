syntax = "proto3";

package proto;

service Ping {
  rpc PingMethod (PingMessage) returns (PingResponse);
}

message PingMessage {
  string pingMsg = 1;
}

message PingResponse {
  string response = 1;
}